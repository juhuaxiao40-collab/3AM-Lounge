# BGM管理系统使用说明

## 功能概述

现在你可以为每个剧情节点配置独立的背景音乐（BGM），当玩家进入不同的剧情分支时，BGM会自动切换，并带有淡入淡出效果。

## 使用步骤

### 1. 打开剧情编辑器

打开 `editor.html` 文件

### 2. 上传BGM文件

1. 点击顶部的 **🎵 BGM管理** 按钮
2. 在弹出的窗口中，点击 **📁 点击选择音频文件**
3. 选择你的音频文件（支持 MP3, OGG, WAV 格式）
4. 文件会被转换为Base64格式存储在剧情数据中

### 3. 分配BGM到节点

1. 上传文件后，会显示节点选择列表
2. 勾选你想应用这个BGM的所有节点
3. 可以使用 **全选** 或 **取消全选** 按钮快速操作
4. 点击 **✅ 保存BGM配置**

### 4. 查看和管理BGM

- 在BGM管理器中可以看到所有已配置的BGM
- 每个BGM显示应用的节点列表
- 可以随时删除不需要的BGM配置

### 5. 保存并导出

1. 关闭BGM管理器
2. 点击 **💾 保存到文件** 按钮
3. 下载的 `story.js` 文件会包含所有BGM数据
4. 将新的 `story.js` 替换项目中的文件

## BGM切换逻辑

### 自动切换
- 当玩家进入一个配置了BGM的节点时，会自动切换到该BGM
- 如果连续多个节点使用同一个BGM，音乐会持续播放不中断
- 切换BGM时会有1秒的淡入淡出效果，过渡自然

### 手动控制
- 玩家可以通过右上角的音乐按钮控制BGM开关
- 关闭BGM后，所有音乐停止
- 重新开启BGM后，会播放当前节点对应的BGM

## 使用建议

### BGM分配策略

**方案1：按路线分配**
- 开场节点：温柔的钢琴曲
- 路线A（共鸣路线）：治愈系Lo-fi音乐
- 路线B（距离路线）：空灵的环境音
- 路线C（倾听路线）：深沉的钢琴曲
- 结局节点：各自独特的结尾曲

**方案2：按情绪分配**
- 平静对话：轻柔背景音乐
- 选择时刻：稍微紧张的音乐
- 温暖时刻：治愈系音乐
- 结局：情感升华的音乐

**方案3：按场景分配**
- 深夜开场：夜晚氛围音乐
- 深入交流：亲密感音乐
- 情感高潮：情绪饱满的音乐
- 告别结局：离别感音乐

### 音频文件建议

1. **文件大小**：每个音频文件建议不超过3MB
   - Base64编码会增加约33%的大小
   - 太大的文件会影响加载速度

2. **音频格式**：推荐使用MP3格式
   - 兼容性最好
   - 压缩率高，文件小

3. **音频长度**：建议2-3分钟的循环音乐
   - 设置为循环播放
   - 避免明显的开头和结尾

4. **音量平衡**：所有BGM音量应该保持一致
   - 避免切换时音量突变
   - 游戏中默认音量为30%

## 示例配置

### 示例1：简单配置
```
BGM1 (opening.mp3) → 应用到: start, intro_1, intro_2, intro_3, choice_1
BGM2 (route_a.mp3) → 应用到: path_a_1, path_a_2, path_a_3, choice_a_1
BGM3 (ending.mp3) → 应用到: ending_a_good, ending_a_normal
```

### 示例2：详细配置
```
BGM1 (night_ambient.mp3) → 开场所有节点
BGM2 (warm_piano.mp3) → 路线A所有节点
BGM3 (calm_lofi.mp3) → 路线B所有节点
BGM4 (deep_piano.mp3) → 路线C所有节点
BGM5 (ending_hope.mp3) → 正面结局
BGM6 (ending_calm.mp3) → 平静结局
```

## 技术细节

### 数据存储
- BGM文件以Base64格式存储在 `story.js` 中
- 每个节点通过 `bgm` 字段引用BGM ID
- BGM配置存储在 `bgmConfig` 对象中

### 播放机制
- 使用HTML5 Audio API
- 支持音频淡入淡出
- 自动循环播放
- 智能切换（相同BGM不重新播放）

### 浏览器兼容性
- 支持所有现代浏览器
- Chrome, Firefox, Safari, Edge
- 移动端浏览器也支持

## 注意事项

1. **首次播放限制**：现代浏览器要求用户交互后才能播放音频，所以BGM需要玩家手动开启

2. **文件大小控制**：不要上传过大的音频文件，建议总大小不超过10MB

3. **版权问题**：确保使用的音乐有合法授权或使用免费音乐

4. **测试建议**：配置完成后，完整测试所有路线的BGM切换效果

## 免费音乐资源

- **YouTube Audio Library**: https://studio.youtube.com/
- **Pixabay Music**: https://pixabay.com/music/
- **FreePD**: https://freepd.com/
- **Incompetech**: https://incompetech.com/

## 故障排除

**问题：BGM不播放**
- 检查是否点击了音乐按钮开启BGM
- 检查浏览器控制台是否有错误
- 确认音频文件格式正确

**问题：切换不流畅**
- 检查音频文件是否过大
- 尝试使用更小的文件或更低的比特率

**问题：音量不一致**
- 使用音频编辑软件统一所有BGM的音量
- 推荐使用 Audacity 进行音量标准化
