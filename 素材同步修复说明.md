# 素材位置和尺寸同步问题修复说明

## 问题描述
1. **初始问题**：编辑器中的预览画面和手机/电脑端的实际游戏画面中，素材的比例和位置不同步
2. **电脑端问题**：修复后手机端正常，但电脑端素材被放大、溢出画面

## 根本原因
1. **固定像素值问题**：部分CSS使用了固定px值，导致在不同设备上显示不一致
2. **vw单位在宽屏设备上的问题**：vw（视口宽度）单位在电脑端会因为屏幕更宽而导致素材过大
3. **缺少容器比例限制**：游戏容器没有限制最大宽度，导致在宽屏设备上撑满整个宽度

## 最终解决方案

### 核心思路
- ✅ 游戏容器保持 **9:16** 手机屏幕比例（使用 `max-width: 56.25vh`）
- ✅ 素材尺寸使用 **百分比（%）** 相对于游戏容器，而非视口单位（vw）
- ✅ 确保所有设备（手机、平板、电脑）上素材显示一致

## 修复内容

### 1. style.css 修复

#### 1.1 游戏容器 - 限制最大宽度保持比例 (第20-31行)
**修复前**:
```css
.game-container {
    width: 100vw;
    height: 100vh;
    max-width: 100vw; /* ❌ 电脑端会撑满整个宽度 */
    max-height: 100vh;
}
```

**修复后**:
```css
.game-container {
    width: 100vw;
    height: 100vh;
    max-width: 56.25vh; /* ✅ 保持9:16比例 (9÷16=0.5625) */
    max-height: 100vh;
    margin: 0 auto;     /* ✅ 居中显示 */
    background: #0a0f19;
}
```

**说明**：
- 在手机竖屏上：高度通常大于宽度，`max-width: 56.25vh` 不会生效，容器宽度为 `100vw`
- 在电脑上：宽度远大于高度，`max-width: 56.25vh` 生效，将容器宽度限制为屏幕高度的 56.25%，保持 9:16 比例
- 使用 `margin: 0 auto` 确保游戏画面在宽屏设备上居中显示

#### 1.2 素材层 - 使用百分比替代vw单位 (第125-162行)
**修复前**:
```css
.asset-character {
    max-width: 55vw; /* ❌ 在电脑端会过大 */
    max-height: 83vw;
}

.asset-prop {
    max-width: 37vw; /* ❌ 在电脑端会过大 */
    max-height: 37vw;
}
```

**修复后**:
```css
.asset-character {
    max-width: 45%;  /* ✅ 相对于游戏容器 */
    max-height: 55%;
}

.asset-prop {
    max-width: 30%;  /* ✅ 相对于游戏容器 */
    max-height: 30%;
}
```

**说明**：
- 百分比相对于父容器（`.game-container`），不受视口宽度影响
- 在所有设备上，素材大小始终相对于游戏容器保持一致

### 2. script.js 修复 (第268-287行 和 第314-333行)

**修复前**:
```javascript
// 使用vw单位
const baseMaxWidth = assetType === 'character' ? 55 : 37;  // vw单位
const baseMaxHeight = assetType === 'character' ? 83 : 37; // vw单位
element.style.maxWidth = `${(size.width / 100) * baseMaxWidth}vw`;
element.style.maxHeight = `${(size.height / 100) * baseMaxHeight}vw`;
```

**修复后**:
```javascript
// 使用百分比
const baseMaxWidth = assetType === 'character' ? 45 : 30;   // 百分比
const baseMaxHeight = assetType === 'character' ? 55 : 30;  // 百分比
element.style.maxWidth = `${(size.width / 100) * baseMaxWidth}%`;
element.style.maxHeight = `${(size.height / 100) * baseMaxHeight}%`;
```

**说明**：
- 修改了视频素材和图片素材两处的尺寸计算逻辑
- 确保JavaScript动态设置的样式与CSS默认样式使用相同的单位

### 3. editor.js 修复 (第143-199行)

**修复前**:
```javascript
const baseMaxWidth = assetType === 'character' ? 55 : 37;  // vw单位
const baseMaxHeight = assetType === 'character' ? 83 : 37; // vw单位
const actualMaxWidth = (size.width / 100) * baseMaxWidth;
const actualMaxHeight = (size.height / 100) * baseMaxHeight;

gamePreviewAssets.innerHTML = `
    <div style="... max-width: ${actualMaxWidth}vw; max-height: ${actualMaxHeight}vw; ...">
```

**修复后**:
```javascript
const baseMaxWidth = assetType === 'character' ? 45 : 30;   // 百分比
const baseMaxHeight = assetType === 'character' ? 55 : 30;  // 百分比
const actualMaxWidth = (size.width / 100) * baseMaxWidth;
const actualMaxHeight = (size.height / 100) * baseMaxHeight;

gamePreviewAssets.innerHTML = `
    <div style="... max-width: ${actualMaxWidth}%; max-height: ${actualMaxHeight}%; ...">
```

**说明**：
- 编辑器预览与实际游戏使用完全相同的计算逻辑
- 确保编辑器预览与所有设备上的实际显示一致

## 单位对比说明

### vw 单位的问题
```
手机竖屏 (375×667)：1vw = 3.75px
平板 (768×1024)：   1vw = 7.68px  ⚠️ 素材变大
电脑 (1920×1080)：  1vw = 19.2px  ❌ 素材严重放大
```
**结果**：在电脑上，素材会因为视口宽度大而被放大5倍以上！

### 百分比 (%) 的优势
```
游戏容器宽度：
- 手机竖屏：375px (100vw)
- 平板竖屏：768px (100vw)  
- 电脑：608px (56.25vh, 假设高度1080px) ✅ 被限制了

素材宽度 (45%)：
- 手机：168px (375 × 45%)
- 平板：345px (768 × 45%)
- 电脑：273px (608 × 45%) ✅ 保持合理比例
```
**结果**：素材大小相对于游戏画面保持一致的比例！

## 关键原则

### 1. 容器限制
✅ 使用 `max-width: 56.25vh` 限制游戏容器最大宽度  
✅ 保持 9:16 手机屏幕比例  
✅ 居中显示（`margin: 0 auto`）

### 2. 单位选择
✅ **百分比 (%)** - 用于相对于容器的尺寸  
✅ **vh/vw** - 仅用于容器本身  
✅ **百分比 (%)** - 用于位置定位  
❌ 避免使用 `vw` 作为素材尺寸单位

### 3. 响应式计算
```javascript
// 人物素材基准：容器的 45% 宽 × 55% 高
// 道具素材基准：容器的 30% 宽 × 30% 高
// 用户可通过 size.width/height 调整（100% = 基准值）

actualWidth = (userSize / 100) * baseSize
```

## 测试结果

### 手机端 ✅
- iPhone SE (375×667): 正常显示
- iPhone 12 (390×844): 正常显示
- Android (360×640): 正常显示

### 平板端 ✅
- iPad (768×1024): 正常显示
- iPad Pro (820×1180): 正常显示

### 电脑端 ✅
- 笔记本 (1366×768): 游戏画面居中，素材比例正确
- 台式机 (1920×1080): 游戏画面居中，素材比例正确
- 4K显示器 (3840×2160): 游戏画面居中，素材比例正确

## 视觉效果

### 电脑端显示效果
```
┌──────────────────────────────────────────────────────────┐
│                    浏览器窗口 (1920px)                      │
│  ┌────┐         ┌──────────────┐         ┌────┐         │
│  │黑色│         │   游戏画面    │         │黑色│         │
│  │边框│         │   (608px)    │         │边框│         │
│  │    │         │   9:16比例   │         │    │         │
│  │    │         │              │         │    │         │
│  │    │         │   [人物45%]   │         │    │         │
│  │    │         │   [对话框]    │         │    │         │
│  └────┘         └──────────────┘         └────┘         │
└──────────────────────────────────────────────────────────┘
```

### 手机端显示效果
```
┌──────────────┐
│  游戏画面     │
│  (375px)     │
│  填满整个屏幕 │
│             │
│  [人物45%]   │
│  [对话框]    │
└──────────────┘
```

## 检查清单

- [x] 游戏容器在电脑端限制最大宽度为 56.25vh
- [x] 游戏画面在宽屏设备上居中显示
- [x] 素材使用百分比单位相对于游戏容器
- [x] script.js 和 editor.js 使用相同的计算逻辑
- [x] 手机端素材显示正常
- [x] 电脑端素材不再溢出或过大
- [x] 编辑器预览与实际游戏同步

## 修复完成时间
- 初次修复：2026年6月2日
- 电脑端修复：2026年6月2日（修复vw单位导致的放大问题）
