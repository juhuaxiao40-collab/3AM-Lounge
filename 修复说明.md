# 素材位置和尺寸同步问题修复说明

## 问题描述
编辑器中的预览画面和手机/电脑端的实际游戏画面中，素材的比例和位置不同步。

## 根本原因
1. **固定像素值问题**：部分CSS使用了固定px值，导致在不同设备上显示不一致
2. **单位不统一**：编辑器预览和实际游戏使用的单位计算方式不完全一致
3. **响应式设计不完善**：容器使用了固定尺寸而非相对单位

## 修复内容

### 1. editor.js 修复
**文件**: `editor.js` (第143-199行)

**修复点**: `updatePositionPreview()` 函数
- ✅ 确保编辑器预览使用与 `script.js` 完全相同的素材定位逻辑
- ✅ 场景素材：使用 `width: 100%; height: 100%` + `object-fit: contain`
- ✅ 人物素材：使用 `left: X%; top: Y%; transform: translate(-50%, -50%)` + `max-width: Xvw; max-height: Xvw`
- ✅ 道具素材：同人物素材
- ✅ 尺寸计算公式与 `script.js` 保持一致：
  ```javascript
  // 人物: baseMaxWidth = 55vw, baseMaxHeight = 83vw
  // 道具: baseMaxWidth = 37vw, baseMaxHeight = 37vw
  actualMaxWidth = (size.width / 100) * baseMaxWidth;
  actualMaxHeight = (size.height / 100) * baseMaxHeight;
  ```

### 2. style.css 修复

#### 2.1 游戏容器响应式优化 (第20-38行)
**修复前**:
```css
.game-container {
    width: 100%;
    max-width: 540px;
    height: 0;
    padding-bottom: 177.78%; /* 固定9:16比例 */
}
```

**修复后**:
```css
.game-container {
    width: 100vw;
    height: 100vh;
    max-width: 100vw;
    max-height: 100vh;
}
```
- ✅ 使用视口单位确保填满整个屏幕
- ✅ 不再强制固定比例，适配各种屏幕尺寸

#### 2.2 素材层响应式优化 (第125-162行)
**修复前**:
```css
.asset-character {
    bottom: 120px; /* 固定像素 ❌ */
    left: 50%;
    transform: translateX(-50%);
    max-height: 70%; /* 相对父容器 ❌ */
}

.asset-prop {
    /* 道具位置可自定义 - 无默认值 ❌ */
}
```

**修复后**:
```css
.asset-character {
    bottom: 25%; /* 百分比 ✅ */
    left: 50%;
    transform: translateX(-50%);
    max-width: 55vw; /* vw单位 ✅ */
    max-height: 83vw;
}

.asset-prop {
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    max-width: 37vw; /* vw单位 ✅ */
    max-height: 37vw;
}
```
- ✅ 所有素材使用百分比和vw单位
- ✅ 与 `script.js` 中的计算逻辑完全一致

#### 2.3 对话框响应式优化 (第414-444行)
**修复前**:
```css
.dialogue-box {
    padding: 28px 35px; /* 固定像素 ❌ */
}
```

**修复后**:
```css
.dialogue-box {
    padding: 2.5vh 3vw; /* 视口单位 ✅ */
}
```

#### 2.4 选择层响应式优化 (第499-519行)
**修复前**:
```css
.choices-container {
    gap: 16px; /* 固定像素 ❌ */
}
```

**修复后**:
```css
.choices-container {
    gap: 1.5vh; /* vh单位 ✅ */
}
```

#### 2.5 角色容器响应式优化 (第381-412行)
**修复前**:
```css
.character-container {
    height: 80%; /* 相对父容器 ❌ */
}

.character {
    max-width: 450px; /* 固定像素 ❌ */
    max-height: 650px;
}
```

**修复后**:
```css
.character-container {
    height: 70vh; /* vh单位 ✅ */
}

.character {
    max-width: 80vw; /* vw单位 ✅ */
    max-height: 70vh;
}
```

## 关键原则

### 1. 使用相对单位而非固定像素
- ✅ `vw` (视口宽度) - 用于水平尺寸
- ✅ `vh` (视口高度) - 用于垂直尺寸
- ✅ `%` (百分比) - 用于位置定位
- ❌ 避免使用 `px` 作为主要定位和尺寸单位

### 2. 位置定位公式
```css
/* 居中定位 */
left: 50%;
top: 50%;
transform: translate(-50%, -50%);

/* 底部居中 */
left: 50%;
bottom: 0;
transform: translateX(-50%);
```

### 3. 尺寸计算公式
```javascript
// 人物素材
const baseMaxWidth = 55;  // vw
const baseMaxHeight = 83; // vw
maxWidth = `${(size.width / 100) * baseMaxWidth}vw`;
maxHeight = `${(size.height / 100) * baseMaxHeight}vw`;

// 道具素材
const baseMaxWidth = 37;  // vw
const baseMaxHeight = 37; // vw
maxWidth = `${(size.width / 100) * baseMaxWidth}vw`;
maxHeight = `${(size.height / 100) * baseMaxHeight}vw`;
```

## 测试建议

### 1. 编辑器预览测试
1. 打开 `editor.html`
2. 上传一个人物素材
3. 调整位置滑块（X: 50%, Y: 80%）
4. 调整大小滑块（W: 100%, H: 100%）
5. 观察游戏界面预览区域中素材的位置和大小

### 2. 实际游戏测试
1. 在编辑器中保存素材配置并导出JSON
2. 打开 `index.html` 运行游戏
3. 检查素材在游戏中的位置和大小是否与编辑器预览一致

### 3. 多设备测试
- 📱 手机竖屏 (375x667, 390x844, 414x896)
- 📱 手机横屏
- 💻 平板 (768x1024, 820x1180)
- 💻 笔记本 (1366x768, 1920x1080)
- 🖥️ 桌面大屏 (2560x1440, 3840x2160)

### 4. 检查清单
- [ ] 场景素材填满整个游戏区域
- [ ] 人物素材底部居中显示
- [ ] 道具素材按指定位置显示
- [ ] 对话框在屏幕底部不被遮挡
- [ ] 选择按钮在对话框上方显示
- [ ] 不同设备上素材比例保持一致
- [ ] 编辑器预览与实际游戏完全同步

## 注意事项

### 必须包含viewport meta标签
确保 `index.html` 和 `editor.html` 都包含：
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

✅ **已确认**: 两个HTML文件都已包含此标签

### 浏览器兼容性
- Chrome/Edge: ✅ 完全支持
- Safari: ✅ 完全支持
- Firefox: ✅ 完全支持
- 移动端浏览器: ✅ 完全支持

## 参考修改建议文档
本次修复完全基于 `修改建议.txt` 中的指导原则：
- ✅ 不使用固定px定位素材
- ✅ 改用vw、vh、%相对单位
- ✅ 使用 `left: 50%; transform: translateX(-50%)` 实现居中
- ✅ 使用 `height: Xvh` 确保不同设备自动适配

## 修复完成时间
2026年6月2日
