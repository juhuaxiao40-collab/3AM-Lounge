# 项目部署指南

## 方案1：GitHub Pages 部署（推荐）

### 步骤1：准备GitHub仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角 "+" → "New repository"
3. 仓库名称填写：`3am-lounge`（或任意名称）
4. 选择 "Public"（公开）
5. 点击 "Create repository"

### 步骤2：上传项目文件

**方法A：使用Git命令行**

```bash
# 在项目文件夹中打开终端
cd "D:\凌晨三点的休息室"

# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 连接到GitHub仓库（替换YOUR_USERNAME为你的GitHub用户名）
git remote add origin https://github.com/YOUR_USERNAME/3am-lounge.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

**方法B：使用GitHub网页上传**

1. 在GitHub仓库页面，点击 "uploading an existing file"
2. 将以下文件拖拽上传：
   - index.html
   - style.css
   - script.js
   - story.js
   - editor.html
   - editor.js
   - README.md
3. 点击 "Commit changes"

### 步骤3：启用GitHub Pages

1. 在仓库页面，点击 "Settings"
2. 左侧菜单找到 "Pages"
3. 在 "Source" 下拉菜单选择 "main" 分支
4. 点击 "Save"
5. 等待几分钟，页面会显示访问链接

**你的网站地址将是：**
```
https://YOUR_USERNAME.github.io/3am-lounge/
```

---

## 方案2：Netlify 部署（更简单）

### 步骤1：注册Netlify

1. 访问 [Netlify](https://www.netlify.com/)
2. 点击 "Sign up" 注册（可以用GitHub账号登录）

### 步骤2：部署网站

**方法A：拖拽部署**

1. 登录后，点击 "Add new site" → "Deploy manually"
2. 将整个项目文件夹拖拽到页面中
3. 等待部署完成
4. Netlify会自动生成一个链接，例如：`https://random-name-123456.netlify.app`

**方法B：连接GitHub**

1. 点击 "Add new site" → "Import an existing project"
2. 选择 "GitHub"
3. 授权并选择你的仓库
4. 点击 "Deploy site"

### 步骤3：自定义域名（可选）

1. 在站点设置中，点击 "Domain settings"
2. 点击 "Options" → "Edit site name"
3. 修改为你想要的名称，例如：`3am-lounge.netlify.app`

---

## 方案3：Vercel 部署

### 步骤1：注册Vercel

1. 访问 [Vercel](https://vercel.com/)
2. 用GitHub账号登录

### 步骤2：部署

1. 点击 "Add New" → "Project"
2. 选择你的GitHub仓库
3. 点击 "Deploy"
4. 等待部署完成

**你的网站地址：**
```
https://3am-lounge.vercel.app
```

---

## 方案4：本地服务器（临时分享）

如果只是临时分享给朋友测试：

### 使用Python启动本地服务器

```bash
# 在项目文件夹中打开终端
cd "D:\凌晨三点的休息室"

# Python 3
python -m http.server 8000

# 或 Python 2
python -m SimpleHTTPServer 8000
```

然后访问：`http://localhost:8000`

### 使用内网穿透工具分享

1. 安装 [ngrok](https://ngrok.com/)
2. 运行：
```bash
ngrok http 8000
```
3. ngrok会生成一个临时公网链接，可以分享给别人

---

## 推荐方案对比

| 方案 | 优点 | 缺点 | 适合场景 |
|------|------|------|----------|
| **GitHub Pages** | 完全免费，稳定，有版本控制 | 需要学习Git | 长期使用，开源项目 |
| **Netlify** | 最简单，拖拽即可，自动HTTPS | 免费版有流量限制 | 快速部署，个人项目 |
| **Vercel** | 速度快，自动部署 | 需要GitHub账号 | 现代化项目 |
| **本地+ngrok** | 无需注册，即时分享 | 临时链接，不稳定 | 临时测试分享 |

---

## 注意事项

### 文件大小问题

如果你的 `story.js` 文件包含Base64编码的BGM音频，文件会很大（5.5MB）。

**建议：**

1. **分离BGM文件**：将BGM文件单独上传到云存储
2. **使用外部音频链接**：修改代码使用URL而不是Base64
3. **压缩音频**：使用更低的比特率

### 如果需要分离BGM

我可以帮你修改代码，让BGM使用外部链接而不是Base64编码，这样可以大大减小文件大小。

---

## 快速开始（最简单方法）

**推荐：Netlify拖拽部署**

1. 访问 https://app.netlify.com/drop
2. 将项目文件夹拖拽到页面
3. 等待30秒
4. 获得永久链接！

就这么简单！
